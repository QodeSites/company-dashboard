# ── Byte‐compiled / optimized Python files ───────────────────────────────────
__pycache__/
*.py[cod]
*$py.class

# ── C extensions ─────────────────────────────────────────────────────────────
*.so

# ── Virtual environments ─────────────────────────────────────────────────────
venv/
ENV/
env/
.venv/
pip‐env‐toml‐lock‐files‐etc/

# ── Distribution / packaging ─────────────────────────────────────────────────
.Python
build/
develop‐eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg‐info/
.installed.cfg
*.egg
pip‐log.txt
pip‐delete‐this‐directory.txt

# ── Test / coverage output ────────────────────────────────────────────────────
htmlcov/
.tox/
.nox/
.coverage
coverage.xml
*,cover
.cache
.pytest_cache/
.pytest_cache/*

# ── Jupyter Notebook checkpoints ──────────────────────────────────────────────
.ipynb_checkpoints/

# ── Environment files (secrets, keys) ────────────────────────────────────────
.env
.env.*.local

# ── IDE / editor folders ─────────────────────────────────────────────────────
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*.swn
*.kate-swp

# ── Mac / Linux hidden system files ──────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Prisma (if you’re using SQLite or local DB files) ────────────────────────
# Ignore any locally generated DB file (e.g. SQLite) inside prisma/
# (If you want to commit your Prisma schema and migration files, do NOT ignore the entire folder.)
prisma/*.db
prisma/*.sqlite

# ── Node.js artifacts (you have node_modules/ in your backend) ───────────────
node_modules/

# ── Logs and runtime artifacts ───────────────────────────────────────────────
*.log
*.out
*.err
pip-log.txt
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Docker / build artifacts ─────────────────────────────────────────────────
# (If you generate images/manifests or have local Docker build files.)
Dockerfile
docker-compose.override.yml
docker-compose.local.yml
*.tar
*.img

# ── Miscellaneous ────────────────────────────────────────────────────────────
*.pyproj
*.project
*.cproject
*.classpath
